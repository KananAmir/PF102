const Loading = () => {
    return (
        <div style={{ display: "flex", justifyContent: " center" }}>
            <p style={{ fontSize: "3rem" }}>LOADDING...</p>
        </div>
    )
}

export default Loading