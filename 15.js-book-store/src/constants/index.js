const BASE_URL = "https://book-store-api-liard-three.vercel.app/";

const endpoints = {
  books: "books",
  genres: "genres",
  users: "users",
  orders: "orders",
};

export { endpoints };
export default BASE_URL;
